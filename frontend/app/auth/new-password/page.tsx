import { Metadata } from "next";

export const metadata: Metadata = {
  title: "New Password",
};

export default function Page() {
  return (
    <div className="card  w-full">
      check if token is valid to restore the password
    </div>
  );
}
